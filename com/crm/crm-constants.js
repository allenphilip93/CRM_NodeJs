define(function(){

  var CONSTANTS = {};

  CONSTANTS.app_version = 'v1';
  CONSTANTS.app_version_path = '/' + CONSTANTS.app_version;

  CONSTANTS.mongodb_connection_active = 1;

  return CONSTANTS;
});